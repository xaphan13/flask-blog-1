

art1 = """
Первым шагом является создание глобального логгера, 
который будет использоваться во всех процессах. 
Этот логгер настраивается с определенным уровнем логирования.
"""


art2 = r"Первым шагом является создание глобального логгера, " \
        r"который будет использоваться во всех процессах. " \
        r"Этот логгер настраивается с определенным уровнем логирования." \
        r"def setup_listener(\"name_file=log\\worker_log.log  log_queue = Queue()"


art3 = r"""
<div class="container">
    <h1>Централизованное логирование в много процессном приложении на Python</h1>
    
    <h2>Введение</h2>
    <p>В современном мире много процессные приложения становятся все более распространенными. Они позволяют эффективно использовать многоядерные процессоры и обрабатывать большие объемы данных параллельно. Однако, когда приложение разбивается на множество процессов, управление логированием становится сложной задачей. В этой статье мы рассмотрим, как настроить централизованное логирование в многопроцессном приложении на Python с использованием модулей <code>logging</code> и <code>multiprocessing</code>.</p>

    <h2>Зачем нужно централизованное логирование?</h2>
    <p>Централизованное логирование позволяет собирать сообщения из всех процессов в одном месте, что упрощает мониторинг и отладку приложения. Без централизованного подхода каждый процесс может записывать логи в отдельный файл, что приводит к фрагментации информации и сложности анализа.</p>

    <h2>Используемые библиотеки</h2>
    <ol>
        <li><strong>logging</strong>: Основной модуль для логирования в Python. Он предоставляет гибкие возможности для настройки уровней логирования, форматов сообщений и обработчиков.</li>
        <li><strong>logging.handlers</strong>: Содержит различные обработчики для логирования, такие как <code>QueueHandler</code> и <code>QueueListener</code>.</li>
        <li><strong>multiprocessing.Queue</strong>: Очередь, используемая для передачи сообщений между процессами.</li>
    </ol>

    <h2>Принципы написания подобного кода</h2>

    <h3>1. Создание глобального логгера</h3>
    <p>Первым шагом является создание глобального логгера, который будет использоваться во всех процессах. Этот логгер настраивается с определенным уровнем логирования (например, <code>INFO</code>).</p>
    
    <pre><code class="language-python">import logging

# Имя логгера, который будет использоваться в системе логирования
name_logger = "worker_logger"

# Создаем глобальный логгер с именем "worker_logger"
logger = logging.getLogger(name_logger)

# Устанавливаем уровень логирования на INFO, чтобы записывать сообщения с уровнем INFO и выше
logger.setLevel(logging.INFO)</code></pre>

    <h3>2. Настройка слушателя для очереди логов</h3>
    <p>Следующим шагом является настройка слушателя, который будет записывать сообщения из очереди в файл. Слушатель использует <code>QueueListener</code> и <code>FileHandler</code>.</p>
    <pre><code class="language-python">import logging.handlers
from multiprocessing import Queue

def setup_listener(name_file=\"log\\worker_log.log\"):
    ""\"
    Настраивает слушателя для очереди логов.

    :param name_file: Путь к файлу, в который будут записываться логи (по умолчанию name_log_file).
    :return: Кортеж из очереди логов и слушателя.log_queue = Queue()

    # Создаем обработчик, который будет записывать логи в файл
    handler = logging.FileHandler(name_file)

    # Создаем слушателя, который будет слушать очередь и передавать сообщения в обработчик
    listener = logging.handlers.QueueListener(log_queue, handler)

    # Запускаем слушателя
    listener.start()

    # Возвращаем очередь и слушателя
    return log_queue, listener</code></pre>

    <h3>3. Форматы сообщений</h3>
    <p>Определяем различные форматы сообщений для логов. Это позволяет настроить вывод информации в удобном для анализа виде.</p>
    <pre><code class="language-python"># Базовый формат сообщений, включающий время, имя логгера, уровень сообщения и само сообщение
form1 = "%(asctime)s - %(name)s - %(levelname)s - %(message)s"

# Формат сообщений, включающий информацию о потоке (thread)
formThr1 = "/* %(asctime)s - %(module)s.%(funcName)s(%(lineno)d) - [%(threadName)s] - [%(thread)d] */   " \
           "\n%(levelname)s: %(message)s"

# Формат сообщений, включающий информацию о процессе (process)
formPrc1 = "/* %(asctime)s - %(module)s.%(funcName)s(%(lineno)d) - [%(processName)s] - [%(process)d] */ " \
           "\n%(levelname)s: %(message)s"</code></pre>

    <h3>4. Настройка логирования для процесса</h3>
    <p>Наконец, настраиваем логирование для каждого процесса. Каждый процесс использует <code>QueueHandler</code> для отправки сообщений в очередь.</p>
    <pre><code class="language-python">def set_logging_process(log_queue):
    ""\"
    Настраивает логирование для процесса.

    :param log_queue: Очередь, в которую будут отправляться сообщения для логирования.
    ""\"
    # Создаем обработчик, который будет передавать сообщения в очередь
    handler = logging.handlers.QueueHandler(log_queue)

    # Устанавливаем уровень логирования на INFO
    handler.setLevel(logging.INFO)

    # Создаем форматтер с использованием форматирования для процесса
    formatter = logging.Formatter(formPrc1)

    # Устанавливаем форматтер для обработчика
    handler.setFormatter(formatter)

    # Добавляем обработчик к логгеру
    logger.addHandler(handler)</code></pre>

    <h2>Алгоритм работы</h2>
    <ol>
        <li><strong>Создание глобального логгера</strong>: Настраиваем логгер с уровнем логирования <code>INFO</code>.</li>
        <li><strong>Настройка слушателя</strong>: Создаем очередь и слушателя, который будет записывать сообщения из очереди в файл.</li>
        <li><strong>Определение форматов сообщений</strong>: Настраиваем различные форматы сообщений для логов.</li>
        <li><strong>Настройка логирования для процесса</strong>: Каждый процесс использует <code>QueueHandler</code> для отправки сообщений в очередь.</li>
        <li><strong>Запуск слушателя</strong>: Слушатель извлекает сообщения из очереди и записывает их в файл.</li>
    </ol>

    <h2>Заключение</h2>
    <p>Централизованное логирование в много процессном приложении на Python — это мощный инструмент для мониторинга и отладки. Используя модули <code>logging</code> и <code>multiprocessing</code>, можно эффективно управлять логированием, собирая сообщения из всех процессов в одном месте. Это упрощает анализ и позволяет быстро находить и исправлять ошибки в приложении.</p>
    <p>Надеемся, что эта статья поможет вам в настройке централизованного логирования в ваших много процессных приложениях. Удачного программирования!</p>
</div>
"""